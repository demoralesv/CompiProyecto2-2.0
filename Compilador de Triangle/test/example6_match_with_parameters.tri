let
  enum Figura (Circulo, Cuadrado);
  var f : Figura;
  var r : Integer;
  var l : Integer;
  var a : Integer
in
  r := 5;
  l := 3; a := 4;
  f := Circulo;
  if f = Circulo then putint(r * 2) else begin end;
  if f = Cuadrado then putint(l * a) else begin end
end
