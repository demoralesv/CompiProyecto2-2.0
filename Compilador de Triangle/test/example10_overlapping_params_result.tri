let
  enum Resultado (Exito, Error);
  var r : Resultado;
  var msg : String;
  func show(res: Resultado): Integer
    if res = Exito then (putstring(msg); 0)
    else (putstring(msg); -1)
  end
in
  msg := "Operación completada";
  r := Exito;
  putint(show(r));
end
