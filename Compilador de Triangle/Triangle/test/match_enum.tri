let
  enum Color (Red, Green, Blue);
  var c : Color;
  var result : Integer
in
  c := Green;
  result := match c of
    case Red: 0
    case Green: 1 
    case Blue: 2
    otherwise: -1
  end;
  putint(result)
end
